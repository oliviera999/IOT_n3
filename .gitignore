# Build / cache (firmwares)
.pio/
.pioenvs/
.piolibdeps/
*.pyc
__pycache__/

# IDE / OS
.vscode/
desktop.ini
*.swp
*.swo
.DS_Store
Thumbs.db

# Fichiers sensibles (ne pas versionner)
credentials.h
config_private.h
secrets.h
.env
.env.local

# Clé privée OTA (ne JAMAIS commiter — la clé publique est committable)
scripts/ota_keys/ota_signing_key.pem
scripts/ota_keys/*.key

# Logs serveur
error_log
*.log
cronlog*.txt

# Rapports de verification serveur (extraits de logs)
scripts/reports/

# Temporaire / backups
_tmp_ffp5cs/
backup/
