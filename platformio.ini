; Fichier factice pour activer l'integration PlatformIO dans Cursor
; (barre d'etat + commandes plugin).
; Ce projet principal n'est pas un projet PlatformIO compilable.

[platformio]
description = IOT_n3 root placeholder for Cursor PlatformIO plugin
default_envs = fake

[env:fake]
platform = native
framework = arduino
build_type = debug

