---
description: Guide pour les firmwares legacy n3pp et msp (ESP32 serre et meteo)
globs: firmwires/n3pp/**/*.cpp,firmwires/n3pp/**/*.h,firmwires/msp/**/*.cpp,firmwires/msp/**/*.h
alwaysApply: false
---

# Firmwares legacy — n3pp et msp

Ces deux firmwares partagent une architecture identique mais avec des capteurs et actionneurs differents. Ils utilisent la bibliotheque **libn3_iot** (drivers DHT, analogique, DS18B20, registre de capteurs).

- **Architecture commune**
  - Cycle : `Wificonnect()` → `variablestoesp()` → lecture capteurs → `datatobdd()` → `sommeil()`
  - Deep sleep avec reveil timer (3000s) + GPIO 4.
  - OLED SSD1306 pour affichage local.
  - ESP Mail Client pour alertes SMTP.

- **Secrets externalises**
  - WiFi SSID/password, cle API, identifiants SMTP sont dans `credentials.h` (non versionne) inclus via `credentials.h.example`.
  - Utiliser `firmwires/credentials.h` (ou `credentials.h.example` comme modele) pour n3pp et msp.

- **Format de communication**
  - POST : `application/x-www-form-urlencoded` (pas JSON).
  - GET : parsing JSON de la reponse du serveur.
  - Endpoints legacy avec `.php` : `/n3pp/n3ppdatas/post-n3pp-data.php`, `/msp1/msp1datas/post-msp1-data.php`.

- **Capteurs — validation requise**
  - Toujours verifier `isnan()` apres lecture DHT.
  - **Fallback si invalide** : temperature = 20.0f, humidite = 50.0f (harmonise msp, n3pp, ffp5cs).
  - Valider les plages de valeurs (temperature -40 a 80°C, humidite 0–100 %).
  - Les capteurs sol analogiques (humidite, luminosite) peuvent retourner 0 ou 4095 si deconnectes.

- **Modifications**
  - Ne pas moderniser ces firmwares en bloc sans plan explicite.
  - Changements acceptables sans plan : correction de bugs, externalisation des secrets, ajout de validation capteurs.
  - Changements qui necessitent un plan : migration JSON, changement d'endpoint, ajout HMAC.
  - Toute modification doit maintenir la compatibilite avec les endpoints serveur existants.

- **Differences**
  - n3pp : board=3, 4 capteurs sol, pompe arrosage, alertes automatiques.
  - msp : board=2, 2 DHT (int/ext), DS18B20, tracker solaire (servos), capteur pluie.
